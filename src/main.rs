mod evasion;

fn main() {
    // Decrypt the package
    // Ensure it matches some checksum
    // Create a child with mapviewoffile and inject into it :^)
    // Now, we have to somehow avoid antivirus software while doing this
}
